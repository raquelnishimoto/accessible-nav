import React from "react"
import { messages } from "./messages";

export const WorkAndExperienceSection = () => {
    return (
        <section>
            <h1>Work and experience</h1>
            <p>{messages.WORK_EXPERIENCE}</p>
        </section>
    );
}